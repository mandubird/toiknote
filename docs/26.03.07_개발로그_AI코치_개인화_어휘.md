# 개발 로그 — 2026.03.07

## 오늘 한 일 요약

8주 프로그램 실효성 검토 → 코드 단절 3곳 수정 → LC 오답 원인 태그 + 어휘 자동 추출 기능 추가

---

## 1. 코드 검토 및 분석

### 발견한 문제
- `startProgramV403()` 실행 후 `buildWeeklyTagPlans()` 호출이 연결되지 않아 **개인화 태그 배정이 작동하지 않았음**
- `generateWeeklyReport()` 안의 ai_feedback이 수동 조합 문자열 (line1~4) 이었고, `generateAICoachComment()`를 호출하지 않았음
- `generateAICoachComment()`는 LLM 연동 stub(고정 5개 문구) 상태였음

### 기획서 검토
- `docs/` 폴더 내 개발명세서 v3 ~ v4.25 전체 검토
- 8주 고정 구조는 **의도적 설계** (v4.03 명세 확인) → 개인화는 태그 선택으로만
- `buildWeeklyTagPlans` (v4.24)는 기획된 기능이었으나 미연결 상태였음
- AI 코치 LLM 연동은 "stub, 실제 연동 시 교체" 명시

### LC 진단 한계 확인
- LC(Part 1~4)는 이미지 촬영 시 음성(지문) 없어서 AI가 오답 원인 분석 불가
- 현재 구조: 파트 분류 + 유형 태그는 가능, 근본 원인 분석은 불가
- **대안**: 유저가 오답 원인을 수동 선택하는 UX

---

## 2. 수정 내역

### `src/services/programService.js`

**① startProgramV403 — 개인화 태그 배정 연결**
```js
// 추가
buildWeeklyTagPlans(userId)
  .then((tagPlans) => saveWeeklyTagPlans(userId, tagPlans))
  .catch(() => {})
```
- 프로그램 시작 직후 약점 Top10 기반으로 주차별 focus_tags 자동 배정

**② saveWeeklyTagPlans — strategy_text 개인화**
```js
// 변경 전: focus_tag_ids, focus_tags, daily_task_count만 업데이트
// 변경 후: strategy_text도 개인화된 문구로 업데이트
const strategyText = `${WEEK_PHASE_LABELS[p.week]} · 집중 태그: ${실제 약점 태그명}`
```

**③ generateWeeklyReport — AI 코치 멘트 연결**
```js
// 변경 전: 수동 line1~4 조합 문자열
// 변경 후: generateAICoachComment() 호출
const aiFeedback = await generateAICoachComment({ userId, weekNumber, ... })
```

**④ import 추가**
```js
import { generateAICoachComment } from './aiCoachService.js'
```

---

### `src/services/aiCoachService.js`

**AI 코치 LLM 실제 연동 (stub → OpenAI gpt-4o-mini)**
- 기존: 5가지 케이스별 고정 문구
- 변경: 정확도 변화, Part7 시간, 약점 태그 TOP3를 컨텍스트로 넣어 LLM 호출
- 형식: 4블록 (`[현재 상태]`, `[약점 포커스]`, `[이번 주 전략]`, `[한 마디]`)
- API 실패 시 fallback 문구로 안전하게 처리
- 케이스 판단(5종) + 톤 가이드는 유지하여 프롬프트에 전달

---

### `src/services/analyzeImage.js`

**keyVocabulary 필드 추출 추가**
- 프롬프트에 `keyVocabulary` 필드 정의 추가 (어휘 문제일 때만, 2개 이하)
- 파싱 로직에 `keyVocabulary` 추출 추가
- 어휘 태그가 있는 문제에서 핵심 단어/표현 자동 추출

---

### `src/components/AnalysisConfirmModal.jsx`

**① LC 오답 원인 선택 추가 (PART_TAG_OPTIONS)**

| 파트 | 추가된 "오답 원인" 카테고리 |
|------|---------------------------|
| Part 1 | 발음혼동, 집중력분산, 어휘몰라서 |
| Part 2 | 발음혼동, 속도못따라감, 어휘몰라서, 집중력분산 |
| Part 3 | 속도못따라감, 집중력분산, 어휘몰라서, 선택지오해 |
| Part 4 | 속도못따라감, 집중력분산, 어휘몰라서, 노트테이킹실패 |

**② Step 1 — 핵심 단어/표현 미리보기 표시**
- `keyVocabulary`가 있으면 "📖 핵심 단어/표현" 섹션 표시
- "저장 시 단어장에 자동 추가돼요" 안내 문구

**③ handleSave — 어휘 태그 자동 병합**
```js
const vocabTags = (initialData?.keyVocabulary || []).map((v) => `어휘:${v}`)
const mergedTags = [...new Set([...tagsArray, ...vocabTags])]
```
- `어휘:단어 (뜻)` 접두어로 일반 태그와 구분해서 저장

---

### `src/pages/WrongNoteDetailPage.jsx`

**핵심 단어 섹션 분리 표시**
- `어휘:` 접두어 태그를 필터링하여 별도 카드로 표시
- 일반 태그와 시각적으로 구분 (amber 배경 카드)
- `mapRowToQuestion`에서 `keyVocabulary` 필드 분리 추출

---

## 3. 변경 파일 목록

| 파일 | 변경 유형 |
|------|----------|
| `src/services/programService.js` | 기능 연결 (3곳) + 개인화 로직 추가 |
| `src/services/aiCoachService.js` | stub → OpenAI LLM 실제 연동 |
| `src/services/analyzeImage.js` | keyVocabulary 필드 추출 추가 |
| `src/components/AnalysisConfirmModal.jsx` | LC 오답 원인 태그 + 어휘 미리보기 + 저장 처리 + **즉각 코치 팁** |
| `src/pages/WrongNoteDetailPage.jsx` | 핵심 단어 섹션 분리 표시 |

---

### `src/components/AnalysisConfirmModal.jsx` (추가 — 즉각 코치 팁)

**LC 오답 원인 선택 시 즉각 AI 코치 팁 표시**
- `LC_COACH_TIPS` 정적 매핑 추가 (6종)
  - 발음혼동 → 딕테이션 연습 권고
  - 집중력분산 → 선택지 미리 훑기 권고
  - 어휘몰라서 → 단어장 추가 + 복습 권고
  - 속도못따라감 → 1.2배속 쉐도잉 권고
  - 선택지오해 → 선택지 키워드 선체크 권고
  - 노트테이킹실패 → 숫자/장소/시간 메모 권고
- Step 2에서 Part 1~4 오답 원인 선택 즉시 파란 카드(`bg-blue-50`)로 코치 조언 표시
- API 호출 없음, 정적 매핑으로 즉각 반응

---

## 4. 미완료 / 향후 과제

- LC 오답 원인 태그가 `user_tag_stats`의 weakness_score에 반영되는지 확인 필요
- `어휘:` 접두어 태그를 모아 "내 단어장" 페이지로 표시하는 뷰 미구현
- `buildWeeklyTagPlans` 호출은 비동기(fire-and-forget)라 완료 전에 페이지 로드될 수 있음 → 로딩 상태 처리 고려
- AI 코치 멘트 품질은 `user_tag_stats` 데이터 축적량에 비례 (초반 사용자는 fallback 많이 발동)

---
---

# 개발 로그 — 2026.03.07 (2차)

## 오늘 한 일 요약

Clear 기능 완료 + 파트 지도 신규 구현 + 랜딩페이지 텍스트 전면 정리 + AI 진단 섹션 리디자인 + v4.27 압축 모드 시스템 구현 (시험일 변경 버튼 + buildCompressedPlan + 블록 카드 UI)

---

## 5. Clear 기능 마무리

### `src/services/programService.js`

**generateWeeklyReport — cleared_count 집계 추가**
```js
import { countClearedThisWeek } from './clearWrongAnswer.js'

const weekStart = startSnap?.created_at
  ? new Date(startSnap.created_at)
  : new Date(Date.now() - 7 * 24 * 60 * 60 * 1000)
const weekEnd = new Date()
const clearedThisWeek = await countClearedThisWeek(userId, weekStart, weekEnd).catch(() => 0)

// payload에 추가
cleared_count: clearedThisWeek
```
- `startSnap` 기준으로 주간 클리어 수 집계, 스냅샷 없으면 7일 전 기준 폴백

**getDashboardSummary — 파트별 클리어 수 집계 추가**
```js
const { data: clearedRows } = await supabase
  .from('wrong_answers').select('part_number')
  .eq('user_id', userId).not('cleared_at', 'is', null)

const clearedPerPart = { 1: 0, ... 7: 0 }
clearedRows?.forEach(row => { clearedPerPart[p] += 1 })
// 반환: part_counts, cleared_per_part
```

### `src/pages/ProgramPage.jsx`

**주간 리포트 cleared_count 표시**
```jsx
{r.cleared_count > 0 && (
  <span className="text-emerald-600 ml-1">· 클리어 {r.cleared_count}문제</span>
)}
```

---

## 6. 파트 지도 (Part Map) 신규 구현

### `src/pages/DashboardPage.jsx`

**PART_META 상수 + getPartColor() 함수**
- Part 1~7 이름/섹션(LC/RC) 정의
- 오답 잔여 수 기준 색상 코딩:
  - 잔여 0 → emerald (완료)
  - 잔여 1~5 → yellow
  - 잔여 6~15 → orange
  - 잔여 16+ → red
  - 오답 없음 → gray

**PartMapSection 컴포넌트**
- LC (Part 1~4) / RC (Part 5~7) 2열 그리드
- 각 파트 버튼 클릭 → `/home?part=N` 이동

### `src/pages/HomePage.jsx`

**URL query param `?part=N` 연동**
```js
const [searchParams] = useSearchParams()
useEffect(() => {
  const n = parseInt(searchParams.get('part'), 10)
  if (n >= 1 && n <= 7) setSelectedPart(`Part ${n}`)
}, [searchParams])
```

---

## 7. 랜딩페이지 텍스트 전면 정리

### "8주 / 60일" → "100일" 일괄 교체

| 파일 | 변경 내용 |
|------|---------|
| `LandingPage.jsx` | "8주 집중 관리 시스템" → "100일 집중 관리 시스템", "8주 프로그램 완료 시" → "100일 프로그램 완료 시" |
| `DashboardPage.jsx` | "8주 프로그램" → "100일 프로젝트" (2곳) |
| `DiagnosticPage.jsx` | "8주 프로그램은 결제 후 Week1이 시작돼요" → "100일 프로젝트는 결제 후 시험일을 입력하면 시작돼요" |
| `LandingDetailPage.jsx` | "60일 전략", "60일 후", "60일 집중 관리 시스템" → "100일" 계열로 일괄 |
| `SocialProofBar.jsx` | "60일" → "100일" |
| `cta.js` | `START_PROGRAM: '60일 전략 시작하기'` → `'100일 프로젝트 시작하기'` |
| `ReviewForm.jsx` | `'8주 루틴 자동 생성'` → `'100일 루틴 자동 생성'` |
| `UpgradePage.jsx` | "프로젝트이 진행 중이에요" → "프로젝트가 진행 중이에요" (조사 오류 수정) |

### 랜딩 "올바른 방법" 문구 교체

- "Part 7 시간 단축 훈련" → "파트별 오답 지도로 취약점 한눈에" (미구현 기능 제거)
- "고난도 유형 반복 학습" → "취약 태그 필터 학습" (실제 구현 기능으로 교체)

---

## 8. 랜딩페이지 AI 진단 섹션 리디자인

### `src/pages/LandingPage.jsx`

**변경 전**: mock 인터랙티브 폼 (목표 점수 입력 → 진단 결과 표시 → "4주 후 +34점, 8주 후 +88점")
- 미구현 기능을 실제처럼 보여주는 신뢰 리스크 존재

**변경 후**: 정적 2단계 구조
- **Step 1**: 정적 예시 카드 (650점 → +250점, 약점 3개 표시, "80점+ 상승 사례 다수")
- **Step 2**: CTA 버튼 "📸 무료로 오답 분석 시작하기 →" → `/home` or `/settings`

```jsx
// 제거된 상태
// useState, handleDiagnosis, DEFAULT_WEAKNESSES, currentScore, targetScore, diagnosisResult

// 추가된 정적 카드
<div className="rounded-xl border-2 border-primary-100 bg-primary-50 p-5">
  <p>📊 AI 진단 예시</p>
  현재 예상 점수 650점 / 목표까지 +250점
  약점: Part 5 기초 문법 부족 / Part 7 시간 관리 / Part 2 우회답변
  💡 약점 집중 개선 시 <strong>80점+ 상승 사례 다수</strong>
</div>
```

---

## 9. v4.27 — 압축 모드 시스템 구현

### 배경

- 시험일이 56일 미만인 사용자는 8주(56일) 플랜을 그대로 따라갈 수 없음
- DB는 week 단위 그대로 유지, 화면에서 block 단위로 동적 재표시
- 압축 비율: `56 / days_left`

---

### `src/services/programService.js`

**① calcDaysUntil(dateStr) — 내부 헬퍼**
- 날짜까지 남은 일수 계산 (서비스 레이어 전용)

**② buildCompressedPlan(plans, examDate) — export**
- 순수 함수 (DB 호출 없음)
- D-day 구간별 블록 병합 및 cap 적용

| D-day | 모드 | 블록 구조 | cap |
|-------|------|-----------|-----|
| D-57+ | 정상 | null 반환 | — |
| D-35~56 | light | [1,2] [3,4] [5,6] [7,8] | 35 |
| D-21~34 | medium | [1,2,3] [4,5,6] [7,8] | 35 |
| D-20- | emergency | 단일 survival block | 28 |

```js
// 핵심 공식
const compressionRatio = 56 / days_left
const daily = Math.min(Math.round(avgBase * compressionRatio), DAILY_CAP)
const durationDays = Math.round(daysLeft * weekNums.length / 8)
```

**반환 View Model:**
```js
{
  block_id: 1,
  label: '집중 블록 1',
  week_label: 'Week 1~2',
  original_weeks: [1, 2],
  duration_days: 10,
  focus_tags: [...merged],
  focus_parts: [...merged],
  strategy_text: 'RC 집중 1주차 · RC 집중 2주차',
  daily_task_count: 31,  // cap 적용
  tier: 'light',
}
```

**③ updateExamDate(userId, examDate) — export**
- `exam_date` 업데이트 + `program_end_date` 재계산
- `program_start_date` + 100일(+bonus_days) 이내로 자동 조정

---

### `src/pages/ProgramPage.jsx`

**① getCompressionTier(dday) 헬퍼 + COMPRESSION_INFO 상수**
- 정적 `text` → 동적 `getText(dday, blockCount)` 함수로 교체
- 배너 문구 v2 UX 방향: "시험까지 N일 남았습니다. AI가 8주 플랜을 M개 집중 블록으로 최적화했습니다."

**② 시험일 변경 버튼 (active 상태에서만)**
- D-day 배너 우측 상단 "시험일 변경" 버튼
- 클릭 시 인라인 date picker 표시 → 저장 → `updateExamDate` 호출 → 화면 갱신
- 새 시험일 미리보기: "변경 후 D-N일 남음"

**③ 현재 진행 카드 — 압축/일반 모드 자동 전환**

```jsx
// compressedBlocks 있으면 블록 모드, 없으면 기존 주차 모드
{compressedBlocks && currentBlock ? (
  // 압축 모드: 집중 블록 N, Week X~Y, N일 집중, cap 적용 문항수
  // + 블록 타임라인 (① ─ ② ─ ③ ─ ④)
) : (
  // 일반 모드: 기존 N주차 표시
)}
```

**④ 블록 타임라인 시각화**
- 완료 블록: emerald ✓
- 현재 블록: primary (ring 강조)
- 미래 블록: gray

---

## 10. 변경 파일 목록 (2차)

| 파일 | 변경 유형 |
|------|----------|
| `src/services/programService.js` | cleared_count 집계 + getDashboardSummary 파트 데이터 + buildCompressedPlan + updateExamDate 추가 |
| `src/services/clearWrongAnswer.js` | (기존) countClearedThisWeek 활용 |
| `src/pages/ProgramPage.jsx` | 시험일 변경 버튼 + 압축 배너 + 블록 카드 UI 전면 교체 |
| `src/pages/DashboardPage.jsx` | Part Map (파트 지도) 추가 + "8주" → "100일" |
| `src/pages/HomePage.jsx` | `?part=N` URL query param 연동 |
| `src/pages/LandingPage.jsx` | AI 진단 섹션 리디자인 + 텍스트 정리 |
| `src/pages/DiagnosticPage.jsx` | "8주" → "100일" 텍스트 |
| `src/pages/LandingDetailPage.jsx` | "60일/8주" → "100일" 텍스트 |
| `src/pages/UpgradePage.jsx` | 조사 오류 수정 |
| `src/components/landing/SocialProofBar.jsx` | "60일" → "100일" |
| `src/constants/cta.js` | START_PROGRAM 텍스트 교체 |
| `src/components/ReviewForm.jsx` | "8주" → "100일" |

---

## 11. 미완료 / 향후 과제

- Supabase 마이그레이션 실행 필요: `20260307000001` (exam_date), `20260307000002` (cleared_at, cleared_count)
- 압축 모드에서 "이 구간 완료" 버튼이 주차 단위로 어드밴스 → 블록 완료 판단 로직 미세 조정 여지 있음
- `buildCompressedPlan` survival 모드에서 strategy_text가 고정 문구 → 실제 사용자 약점 태그 기반으로 동적 생성 고려
- 학습 시간 cap(90분) 미구현 — 타이머 기능 추가 시 연동 예정
- Part Map에서 파트 클릭 시 HomePage 이동 후 필터 자동 적용 확인 필요 (useSearchParams 연동)
